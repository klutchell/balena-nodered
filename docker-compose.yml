version: '2.1'

volumes: 
  node-red-data:
  ssh-data:
  
services:

  node-red:
    build: ./node-red
    ports:
      - '80:1880'
    volumes:
      - 'node-red-data:/data'

  ssh:
    build: ./ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'
    volumes_from:
      - node-red

  cloud9:
    build: ./cloud9
    ports:
      - '8080:8080'
    volumes_from:
      - ssh
      - node-red

