version: '2.1'

volumes: 
  node-red-data:
  mosquitto-config:
  mosquitto-data:
  hue2mqtt-config:
  cloud9-workspace:
  ssh-data:
  
services:

  node-red:
    build: ./node-red
    ports:
      - '80:1880'
    volumes:
      - 'node-red-data:/data'

  mosquitto: 
    image: mbixtech/arm32v7-mosquitto
    ports:
      - '1883:1883'
    volumes:
      - 'mosquitto-config:/mosquitto/config'
      - 'mosquitto-data:/mosquitto/data'

  hue2mqtt:
    build: ./hue2mqtt
    volumes:
      - 'hue2mqtt-config:/root/.hue2mqtt'

  huesensors2mqtt:
    build: ./huesensors2mqtt

  ssh:
    build: ./ssh
    ports:
      - '22:22'
    volumes:
      - 'ssh-data:/root/.ssh'
      - 'cloud9-workspace:/cloud9-workspace'
      - 'node-red-data:/node-red-data'
      - 'mosquitto-config:/mosquitto-config'
      - 'hue2mqtt-config:/hue2mqtt-config'

  cloud9:
    build: ./cloud9
    ports:
      - '8080:8080'
    volumes:
      - 'ssh-data:/root/.ssh'
      - 'cloud9-workspace:/cloud9-workspace'
      - 'node-red-data:/node-red-data'
      - 'mosquitto-config:/mosquitto-config'
      - 'hue2mqtt-config:/hue2mqtt-config'

